\documentclass{article}
\usepackage[english]{babel}
\usepackage[letterpaper,top=2cm,bottom=2cm,left=3cm,right=3cm,marginparwidth=1.75cm]{geometry}

% Useful packages
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{graphicx}
\graphicspath{{figures/}{../wpm/}}
\DeclareGraphicsExtensions{.pdf,.png,.jpg}
\newcommand{\safeincludegraphics}[2][]{%
    \IfFileExists{figures/#2.pdf}{\includegraphics[#1]{figures/#2}}{%
        \IfFileExists{figures/#2.png}{\includegraphics[#1]{figures/#2}}{%
            \IfFileExists{figures/#2.jpg}{\includegraphics[#1]{figures/#2}}{%
                \fbox{\parbox[c][0.25\textheight][c]{0.9\linewidth}{\centering Missing figure:\\ \texttt{\detokenize{#2}}}}%
            }%
        }%
    }%
}
\usepackage{tikz}
\usetikzlibrary{positioning}
\usepackage[colorlinks=true, allcolors=blue]{hyperref}

\title{Wide-Angle Multislice for Electron Microscopy via the Wave Propagation Method}
\author{Author Name}

\begin{document}
\maketitle

\begin{abstract}
Multislice simulation is the standard method for modeling electron beam-sample interactions in transmission electron microscopy (TEM).
The traditional multislice algorithm is calculated using a split-step Fourier method that employs a paraxial (Fresnel) propagator, limiting its accuracy
to small scattering angles (typically $<7^\circ$) and ignoring the change of the electron wavelength inside the sample potential.
These limitations can be significant in scenarios involving high-angle scattering or thick, high-Z samples.
In this work, we present a systematic comparison of four propagation methods of increasing physical rigour:
Fresnel multislice, angular spectrum multislice, the Wave Propagation Method (WPM) from the light optics community, and a Krylov-subspace (Lanczos) forward solver for the Klein-Gordon equation.
We cast the Dirac, Klein-Gordon, and relativistically corrected Schr\"odinger equations into a common Helmholtz form, adapt the WPM to electron microscopy via a differentiable refractive-index binning strategy,
and benchmark all four methods against high-angle reference solutions across z-sampling convergence, convergent-beam diffraction at probe semi-angles up to 100~mrad,
and frozen-phonon Si~[111] simulations.
Our principal finding is that replacing the Fresnel propagation kernel with the exact angular spectrum kernel---a minimal, inexpensive modification to standard multislice---is
sufficient to capture high-angle scattering with accuracy comparable to the far more computationally expensive WPM and Klein-Gordon solvers across all tested regimes.
\end{abstract}

\section{Introduction - Wave Equation}

Transmission electron microscopy has become one of the most powerful tools in condensed-matter physics, materials science, and structural biology, providing direct access to the atomic-scale structure and composition of matter. Modern aberration-corrected instruments routinely achieve sub-angstrom spatial resolution, enabling the imaging of individual atomic columns, the mapping of local chemical environments, and the characterisation of lattice defects and interfaces with an atomic precision that is unavailable to any other technique. Interpreting these data quantitatively is not straightforward, however: extracting atomic positions, strain fields, or chemical composition requires matching experimental images and diffraction patterns against simulated counterparts, and the fidelity of that comparison is bounded by the accuracy of the forward simulation model.


The time-independent wave equation for a relativistic electron traversing an electrostatic potential $V(\mathbf{r})$, neglecting spin, is the Klein-Gordon equation. Written in Helmholtz form, it reads

\begin{equation}
\label{eq:kg_helmholtz_intro}
\nabla^2 \psi + k_0^2\, n^2(\mathbf{r})\, \psi = 0,
\end{equation}

where $k_0 = \sqrt{E^2 - m_0^2 c^4}\,/\,\hbar c$ is the vacuum wavenumber and

\begin{equation}
\label{eq:n_intro}
n(\mathbf{r}) = \sqrt{\frac{\bigl(E - V(\mathbf{r})\bigr)^2 - m_0^2 c^4}{E^2 - m_0^2 c^4}}
\end{equation}

is an effective electron refractive index set by the local electrostatic potential. This expression equals unity in vacuum and departs from it only where the potential is nonzero, encoding the full relativistic energy-momentum relation of the electron in the presence of the specimen.%
\footnote{The relativistically corrected Schr\"{o}dinger equation follows by expanding $(E-V)^2 \approx E^2 - 2EV$ and dropping the $V^2$ term, which is valid when $|V| \ll E$. Substituting into $k_0^2 n^2(\mathbf{r})$ gives $k_0^2 n^2 \approx k_0^2 - 2\gamma m_0 V/\hbar^2$, and the wave equation reduces to $\nabla^2\psi + [k_0^2 - 2\gamma m_0 V/\hbar^2]\,\psi = 0$, the familiar form of the relativistically corrected Schr\"{o}dinger equation.}

The standard numerical framework for solving this equation in TEM is the multislice method, introduced by Cowley and Moodie \cite{cowley1957scattering} and refined into a widely deployed computational tool over the following decades. Its central idea is to divide the specimen into a series of thin slices perpendicular to the beam and propagate the electron wave through them by alternating between a transmission step---in which the wave accumulates a phase shift proportional to the projected electrostatic potential---and a free-space propagation step. This split-step structure arises naturally from the Helmholtz equation~\eqref{eq:kg_helmholtz_intro}. Separating the Laplacian into transverse and longitudinal parts, $\nabla^2 = \partial_z^2 + \nabla_\perp^2$, and rearranging gives
\[
\partial_z^2\psi = -\bigl(k_0^2 n^2(\mathbf{r}) + \nabla_\perp^2\bigr)\psi.
\]
This second-order equation in $z$ factors into forward- and backward-propagating operators; neglecting back-scattering and retaining only the forward factor yields the one-way wave equation
\begin{equation}
\label{eq:one_way_intro}
\partial_z \psi = i\sqrt{k_0^2 n^2(\mathbf{r}) + \nabla_\perp^2}\;\psi,
\end{equation}
whose formal solution over a slice of thickness $\Delta z$ is $\psi(z+\Delta z) = \exp(i\sqrt{k_0^2 n^2 + \nabla_\perp^2}\,\Delta z)\,\psi(z)$. The split-step approximation factors this propagator into a potential term and a free-space diffraction term,
\[
\exp\!\bigl(i\sqrt{k_0^2 n^2(\mathbf{r}) + \nabla_\perp^2}\,\Delta z\bigr) \approx e^{ik_0(n(\mathbf{r})-1)\Delta z} \cdot e^{i\sqrt{k_0^2+\nabla_\perp^2}\,\Delta z},
\]
which is valid when the potential varies slowly over $\Delta z$. Linearising equation~\eqref{eq:n_intro} for $|V|\ll E$ gives $k_0(n-1) \approx \sigma V$, where $\sigma = 2\pi\gamma m_0 e\lambda/h^2$ is the relativistic interaction constant; integrating through the slice then replaces the 3D potential with its column projection $V_\mathrm{proj}(x,y) = \int V\,\mathrm{d}z$. Replacing the free-space factor by the paraxial Fresnel kernel $e^{-ik_\perp^2\Delta z/(2k_0)}$ in Fourier space gives
\begin{equation}
\label{eq:ms_intro}
\psi(x,y,z+\Delta z) \approx e^{i\sigma V_\mathrm{proj}(x,y)}\,
\mathcal{F}^{-1}\!\left[e^{-i\frac{k_\perp^2}{2k_0}\Delta z}\,\mathcal{F}\{\psi\}\right],
\end{equation}
where $V_\mathrm{proj}$ is the projected potential of the slice and $k_\perp^2 = k_x^2 + k_y^2$. Equation~\eqref{eq:ms_intro} is the standard multislice propagator---the workhorse of quantitative TEM simulation. This split-operator structure is computationally efficient, exploits the Fast Fourier Transform (FFT) for both steps, scales favourably with modern GPU hardware, and correctly reproduces the dynamical (multiple) scattering essential for quantitative TEM simulation. The propagation step is in practice implemented using the Fresnel (paraxial) propagator, which is the leading-order approximation to free-space diffraction valid only when all relevant scattering angles are small. In this limit the exact dispersion relation is linearised and the electron wavelength is held constant even inside regions of nonzero potential, where the full equation~\eqref{eq:n_intro} requires it to change. For most conventional conditions---thin specimens, small convergence angles, low-to-moderate atomic numbers---these errors are negligible. For thick, high-Z specimens or large probe convergence angles, however, cumulative high-angle scattering drives the beam into the regime where the linearisation breaks down, and diffracted intensities beyond roughly $7^\circ$ are increasingly misdescribed.

The optics community has developed a family of non-paraxial propagation methods that solve the Helmholtz equation well beyond the Fresnel limit while remaining computationally tractable: the angular spectrum method retains the exact free-space dispersion relation; Pad\'e approximant schemes extend accuracy to successively higher angles through rational approximations to the square-root operator; and the Wave Propagation Method (WPM) \cite{Brenner1993} solves the forward-scattering Helmholtz equation through a Fourier-domain integral that keeps the square-root operator fully intact. Transferring any of these to electron microscopy requires only substituting the electron refractive index of equation~\eqref{eq:n_intro} for its optical counterpart. The present work pursues that programme systematically: we implement and benchmark four propagation methods of increasing physical rigour---standard Fresnel multislice; the angular spectrum method, which replaces only the Fresnel kernel with the exact free-space dispersion relation; the WPM, a high-angle split-step scheme that additionally accounts for the spatially varying electron wavelength inside the potential; and a ground-truth forward solver for the Klein-Gordon equation---and compare their accuracy and computational cost across a range of typical scattering problems in electron microscopy.


\section{Numerical methods}
\label{sec:methods}

We benchmark four numerical schemes for advancing the electron wave through the specimen, each
corresponding to a different level of approximation to the one-way Helmholtz
equation~\eqref{eq:one_way_intro}.\footnote{The full Dirac equation contributes a spin--orbit coupling
$-(i/\hbar c)(\vec\alpha\!\cdot\!\nabla V)\psi$ on the right-hand side of equation~\eqref{eq:kg_helmholtz_intro};
its effect on beam amplitudes at the energies considered here is negligible \cite{RotherScheerschmidt2009},
so we work throughout with the spinless Klein-Gordon equation.}
We describe them in order of decreasing physical rigour: a direct forward-ODE solver for the
full second-order Klein-Gordon equation, the Wave Propagation Method, the angular spectrum
multislice, and---for completeness---the standard Fresnel multislice derived above. The forward-ODE
solver makes no split-step or one-way approximation and serves throughout this paper as the
numerical ground truth against which the split-step schemes are benchmarked. In every scheme the
specimen is divided into thin slices of thickness $\Delta z$ perpendicular to the optic axis and the
wavefunction is advanced slice-by-slice on a transverse $N_x\times N_y$ grid; the methods differ
only in how the slice propagator is constructed.

\subsection{Forward Klein-Gordon ODE ground truth}
\label{sec:kg_ode}

The most accurate scheme retains the full second-order Klein-Gordon equation~\eqref{eq:kg_helmholtz_intro}
without making the one-way approximation~\eqref{eq:one_way_intro}. Separating the Laplacian as
$\nabla^2 = \partial_z^2 + \nabla_\perp^2$, equation~\eqref{eq:kg_helmholtz_intro} reads
\begin{equation}
\label{eq:kg_second_order}
\partial_z^2 \psi \;=\; -\bigl(\nabla_\perp^2 + k_0^2\, n^2(x,y,z)\bigr)\,\psi,
\end{equation}
which we recast as a first-order system in the state $(\psi,\phi)$ with $\phi\equiv\partial_z\psi$,
\begin{equation}
\label{eq:kg_state}
\frac{d}{dz}\begin{pmatrix}\psi\\\phi\end{pmatrix}
\;=\;
\begin{pmatrix} \phi \\ -\bigl(\nabla_\perp^2 + k_0^2 n^2(x,y,z)\bigr)\,\psi \end{pmatrix},
\end{equation}
and integrate along $z$ with an adaptive high-order Runge--Kutta scheme (8th-order Dormand--Prince).
The right-hand side is evaluated in $\mathcal{O}(N\log N)$ per stage by handling each term in its
diagonal basis: $\nabla_\perp^2\psi$ is computed via two FFTs (multiplication by $-k_\perp^2$ in
Fourier space) while $k_0^2 n^2(x,y,z)\,\psi$ is a pointwise real-space multiplication. The
potential is treated as piecewise constant over each slice of thickness $\Delta z$ and the step-size
controller is clipped at the slice boundaries so the solver never steps across a discontinuity in
$n^2(z)$. The wavefunction is initialised with the input probe $\psi(z=0)$ and the longitudinal
derivative
\begin{equation}
\label{eq:kg_phi0}
\phi(z=0) \;=\; \mathcal{F}^{-1}\!\bigl[i\sqrt{k_0^2 - k_\perp^2}\,\mathcal{F}\{\psi(z=0)\}\bigr],
\end{equation}
which enforces purely forward-propagating waves in the vacuum entrance plane.
This can be viewed as a real-space, arbitrary-probe generalisation of the forward-scattering ODE
benchmark used by Rother and Scheerschmidt~\cite{RotherScheerschmidt2009}, without the
periodic-crystal and beam-basis restrictions of that formulation. Because no split-step or paraxial factorisation is applied, the scheme reproduces the full
dispersion relation $k_z=\sqrt{k_0^2 n^2 - k_\perp^2}$ and correctly couples the transverse
Laplacian to the spatially varying potential within each slice. Its cost is dominated by the FFT
pairs of the right-hand side evaluations; the adaptive controller refines the step inside the
specimen, so the ODE solver is typically several times more expensive than the split-step schemes
at matched accuracy, but no more so. We use it as the ground-truth reference throughout this work.

\subsection{Wave Propagation Method}
\label{sec:wpm}

The WPM, introduced by Brenner~\cite{Brenner1993} and developed into a systematic wave-optical
scheme for thick inhomogeneous elements by Schmidt et al.~\cite{Schmidt2016}, starts from the
exact slice propagator associated with the one-way wave equation~\eqref{eq:one_way_intro},
\[
\psi(z+\Delta z) \,=\, \exp\!\Bigl(i\Delta z\sqrt{k_0^2 n^2(x,y)+\nabla_\perp^2}\Bigr)\,\psi(z).
\]
For a homogeneous slice, $n$ is constant and therefore commutes with $\nabla_\perp^2$, so the
square-root operator is diagonal in Fourier space. Each transverse Fourier component then
propagates independently with longitudinal wavevector $k_z=\sqrt{k_0^2 n^2-k_\perp^2}$, and the
one-slice update is obtained by multiplying the angular spectrum by $\exp(i k_z\Delta z)$.
Schmidt et al. derive in full in the appendix of their paper how this homogeneous-medium operator
can be extended to a thick inhomogeneous element by partitioning the transverse plane into
subdomains of nearly constant refractive index and applying the corresponding homogeneous-slice
propagator locally. The resulting approximation can be written in the continuum limit as a mixed
real/Fourier-space propagator,
\begin{equation}
\label{eq:wpm}
\psi(x,y,z+\Delta z) \;=\; \frac{1}{(2\pi)^2}\iint
\tilde\psi(k_x,k_y,z)\,e^{\,i\, k_z(x,y;k_\perp)\,\Delta z}\,
e^{i(k_x x + k_y y)}\,dk_x\,dk_y,
\end{equation}
with the spatially dependent longitudinal wavevector
\begin{equation}
\label{eq:wpm_kz}
k_z(x,y;k_\perp) \;=\; \sqrt{k_0^2\, n^2(x,y) - k_\perp^2}.
\end{equation}
In Schmidt's derivation, this is precisely how one goes beyond the thin-element approximation:
propagation and refraction are treated simultaneously through the local dispersion relation instead
of being factorised into a pure transmission function and a separate vacuum propagator. Unlike the
standard split-step, the WPM kernel depends \emph{simultaneously} on real-space
coordinates (through $n(x,y)$) and Fourier-space coordinates (through $k_\perp$), so the propagator
cannot be evaluated by a single FFT pair. The direct implementation costs $\mathcal{O}(N^2)$ per
slice, which is prohibitive at typical TEM grid sizes. Following \cite{Schmidt2016,DiffWPM2025} we
sample $n(x,y)$ by a small set of reference values $\{n_m\}_{m=1}^{N_\mathrm{bin}}$ so that the WPM
step decomposes into a bank of homogeneous-slice angular spectrum propagations followed by a
spatially weighted recombination,
\begin{equation}
\label{eq:wpm_bank}
\psi(x,y,z+\Delta z) \;\approx\; \sum_{m=1}^{N_\mathrm{bin}} w_m(x,y)\;
\mathcal{F}^{-1}\!\bigl[e^{\,i\Delta z\sqrt{k_0^2 n_m^2 - k_\perp^2}}\,\mathcal{F}\{\psi(x,y,z)\}\bigr],
\end{equation}
where $w_m(x,y)$ interpolates the local refractive index between adjacent bins. This reduces the
per-slice cost to $\mathcal{O}(N_\mathrm{bin}\,N\log N)$. We use the differentiable binning scheme
described in Appendix~\ref{sec:binning}, which selects $\{n_m\}$ on a quadratic grid in
$[n_\mathrm{min},n_\mathrm{max}]$ and uses a smoothstep weight $w_m$ so that the propagator remains
differentiable with respect to the potential. When $n(x,y)$ is constant across the transverse plane
a single bin suffices and the WPM reduces exactly to the angular spectrum method described next.

\subsection{Angular spectrum multislice}
\label{sec:asm}

The angular spectrum method (ASM) replaces the paraxial Fresnel kernel of
equation~\eqref{eq:ms_intro} with the exact free-space dispersion relation while retaining the
split-step structure of standard multislice. The homogeneous-slice propagator in Fourier space is
\begin{equation}
\label{eq:asm_kernel}
\hat P_\mathrm{ASM}(k_\perp;\Delta z) \;=\; \exp\!\bigl(i\Delta z\,\sqrt{k_0^2 - k_\perp^2}\bigr),
\end{equation}
and the one-slice update is
\begin{equation}
\label{eq:asm_step}
\psi(x,y,z+\Delta z) \;\approx\; e^{\,i\sigma V_\mathrm{proj}(x,y)}\;
\mathcal{F}^{-1}\!\bigl[\hat P_\mathrm{ASM}(k_\perp;\Delta z)\,\mathcal{F}\{\psi(x,y,z)\}\bigr].
\end{equation}
The transmission factor is identical to standard multislice. Replacing only the Fresnel kernel
costs nothing: the FFT pair is unchanged and the per-slice cost remains $\mathcal{O}(N\log N)$. The
relationship to Fresnel multislice is made transparent by expanding the square-root operator,
\begin{equation*}
\sqrt{k_0^2 + \nabla_\perp^2} \;=\; k_0 + \frac{\nabla_\perp^2}{2k_0}
- \frac{\nabla_\perp^4}{8k_0^3} + \cdots,
\end{equation*}
where truncating at the second term recovers the Fresnel kernel
$\exp(-ik_\perp^2\Delta z/(2k_0))$, whereas ASM retains the operator to all orders. ASM is the
minimal correction to standard multislice that captures the correct free-space high-angle
propagation while preserving its split-step structure and FFT efficiency. It differs from the WPM
only in that, inside the potential, the wavelength is held at its vacuum value $\lambda_0=2\pi/k_0$
rather than tracking the local $\lambda(x,y)=2\pi/(k_0 n(x,y))$.

\subsection{Fresnel multislice}
\label{sec:fms}

For completeness we recall the standard split-step Fresnel multislice scheme derived in the
Introduction (equation~\eqref{eq:ms_intro}): the slice propagator combines a transmission factor
$\exp(i\sigma V_\mathrm{proj})$ with the paraxial Fresnel kernel
$\exp(-ik_\perp^2\Delta z/(2k_0))$, obtained by truncating the square-root operator at second order
in $k_\perp/k_0$. It is the cheapest of the four schemes---one FFT pair per slice---and the
standard tool of quantitative TEM simulation. Its accuracy is bounded by the paraxial assumption
$k_\perp^2 \ll k_0^2$.

\begin{figure}[ht]
    \centering
    \safeincludegraphics[width=\textwidth,keepaspectratio]{method_summary_standalone}
    \caption{Relationship between the Klein-Gordon/Helmholtz wave equation and the four propagation
             schemes compared in this work. The KG ODE solves the second-order equation directly;
             WPM approximates the one-way square-root operator using local refractive-index
             propagators; angular spectrum multislice is the homogeneous-index propagation limit;
             and Fresnel multislice follows from the small-angle expansion of the angular-spectrum kernel.}
    \label{fig:method_summary}
\end{figure}

\section{Results}

We present four numerical experiments of increasing physical complexity, all comparing
Fresnel multislice (FMS), angular spectrum multislice (ASMS), the Wave Propagation Method (WPM),
and a Krylov--Lanczos forward solver for the Klein-Gordon equation (KG~FWD).
In each case the ground truth is either a converged ODE solution of the full second-order Klein-Gordon equation
(Sections~\ref{sec:verification}--\ref{sec:convergence_angle}) or the KG~FWD Lanczos propagator at matched z-sampling
(Section~\ref{sec:frozen_phonon}).

\subsection{Verification against Rother \& Scheerschmidt (2009)}
\label{sec:verification}

To verify all four propagation methods, we recreated the simulation presented in figure~3 of \cite{RotherScheerschmidt2009},
which tracks the amplitude of a central beam $[0,0]$ and a high-angle beam $[0,28]$ at 135~mrad
after propagation through a thick gold [100] crystal at 300~keV.
The ground truth is a converged ODE solution of the full second-order Klein-Gordon equation.

For the central beam, all methods agree closely.
For the high-angle beam at 135~mrad, the Fresnel multislice solution diverges from the ground truth after approximately five unit cells,
consistent with the findings of \cite{RotherScheerschmidt2009} who attributed the discrepancy to the linearization of the square-root operator.
However, the angular spectrum multislice solution---which differs from Fresnel MS only by replacing the parabolic propagation kernel
$\exp(-i k_\perp^2 \Delta z / 2k_0)$ with the exact kernel $\exp(i\sqrt{k_0^2 - k_\perp^2}\,\Delta z)$---closely tracks the more expensive
WPM and KG solutions. This is the first indication that the angular spectrum kernel alone captures the essential high-angle physics.
Table~\ref{tab:rmse_verification} quantifies this observation with RMSE values computed over the first 25 unit cells against the digitized paper reference.

\begin{figure}[ht]
    \centering
    \safeincludegraphics[width=\textwidth,keepaspectratio]{Au_beam_amplitudes}
    \caption{Amplitude of the central $[0,0]$ and high-angle $[0,28]$ (135~mrad) diffracted beams
             as a function of crystal thickness for Au~[100] at 300~keV, recreating figure~3 of \cite{RotherScheerschmidt2009}.
             The angular spectrum MS (ASMS) closely tracks the WPM and KG ground truth,
             while Fresnel MS (FMS) diverges at high angles.}
    \label{fig:au_beam_amplitudes}
\end{figure}

\begin{table}[ht]
    \centering
    \begin{tabular}{lcc}
        \hline
        \textbf{Method} & \textbf{RMSE $[0,0]$} & \textbf{RMSE $[0,28]$} \\
        \hline
        Fresnel MS       & $8.17\times10^{-3}$  & $5.02\times10^{-3}$ \\
        Angular Spectrum MS & $8.58\times10^{-3}$  & $8.15\times10^{-4}$ \\
        WPM              & $8.40\times10^{-3}$  & $8.65\times10^{-4}$ \\
        KG FWD Lanczos   & $8.50\times10^{-3}$  & $8.60\times10^{-4}$ \\
        Full KG ODE      & $8.45\times10^{-3}$  & $7.77\times10^{-4}$ \\
        \hline
    \end{tabular}
    \caption{RMSE of beam amplitudes versus the digitized reference data of \cite{RotherScheerschmidt2009}
             over the first 25 unit cells.}
    \label{tab:rmse_verification}
\end{table}

\subsection{Z-sampling convergence}
\label{sec:z_sampling}

Having established qualitative agreement, we now test how each method converges as the z-sampling (number of slices per unit cell) is refined.
We propagate a plane wave through ${\sim}100$ unit cells of Au~[110] at 200~keV.
The ground truth is a KG~ODE solution with 128 slices per unit cell and tight tolerances ($\texttt{rtol} = 10^{-9}$, $\texttt{atol} = 10^{-11}$).
For each method, the z-sampling is varied from 1 to 128 slices per unit cell and the relative $L^2$ norm of the
diffraction pattern difference is computed against the ground truth.

Figure~\ref{fig:z_convergence} shows that all four methods converge at a similar rate as the z-sampling is refined,
and that the WPM and KG~FWD~Lanczos methods do not permit coarser z-sampling than standard multislice for the same level of accuracy.
Beyond ${\sim}8$ slices per unit cell, further refinement yields diminishing returns---less than one order of magnitude
improvement in diffraction pattern error despite a large increase in computational cost.
The angular spectrum method achieves comparable accuracy to WPM and KG~FWD at each z-sampling level,
at a fraction of the computational cost.

\begin{figure}[ht]
    \centering
    \safeincludegraphics[width=\textwidth,keepaspectratio]{z_sampling_convergence}
    \caption{Z-sampling convergence for Au~[110] at 200~keV (${\sim}100$ unit cells).
             Left: relative $L^2$ error of the diffraction pattern vs.\ the KG~ODE ground truth (128~sl/cell).
             Right: wall-clock runtime.
             All methods converge at a similar rate; the more expensive WPM and KG solvers offer no z-sampling advantage.}
    \label{fig:z_convergence}
\end{figure}

\subsection{Accuracy versus probe convergence angle}
\label{sec:convergence_angle}

Next, we test each method's accuracy as a function of probe convergence semi-angle for a convergent-beam electron diffraction (CBED) geometry.
A convergent probe with semi-angles ranging from 5 to 100~mrad is propagated through the same Au~[110] crystal at 200~keV,
using 32 slices per unit cell for the test methods and a KG~ODE ground truth with 128 slices per unit cell.

Figure~\ref{fig:angle_convergence} plots the relative $L^2$ diffraction pattern error as a function of semi-angle.
Across the entire angular range, the angular spectrum method provides accuracy comparable to both WPM and KG~FWD~Lanczos,
while the Fresnel propagator shows a growing error at higher convergence angles.
Figure~\ref{fig:cbed_patterns} displays the diffraction patterns at four representative semi-angles (5, 30, 50, and 100~mrad),
confirming that no visually discernible differences exist between angular spectrum MS and the more expensive methods even at 100~mrad.

\begin{figure}[ht]
    \centering
    \safeincludegraphics[width=\textwidth,keepaspectratio]{angle_convergence}
    \caption{Relative $L^2$ error of the CBED diffraction pattern versus the KG~ODE ground truth (128~sl/cell)
             as a function of probe convergence semi-angle for Au~[110] at 200~keV (32~sl/cell for test methods).
             The angular spectrum method tracks the WPM and KG~FWD accuracy throughout.}
    \label{fig:angle_convergence}
\end{figure}

\begin{figure}[ht]
    \centering
    \safeincludegraphics[width=\textwidth,keepaspectratio]{cbed_pattern_grid}
    \caption{CBED patterns for Au~[110] at 200~keV at selected convergence semi-angles (5, 30, 50, 100~mrad).
             Columns: KG~ODE ground truth, Fresnel MS, Angular Spectrum MS, WPM, KG~FWD Lanczos.
             The angular spectrum patterns are visually indistinguishable from the WPM and KG references
             across all tested semi-angles.}
    \label{fig:cbed_patterns}
\end{figure}

\subsection{Frozen phonon CBED --- Si [111]}
\label{sec:frozen_phonon}

Finally, we test each method in a realistic frozen-phonon CBED simulation.
Si~[111] is propagated at 200~keV with a 6.2~mrad convergent probe,
120 unit cells along the beam direction, 16 slices per unit cell, and 16 frozen phonon configurations
($\sigma_\mathrm{Si} = 0.078$~\AA).
For this experiment, the KG~FWD~Lanczos propagator at matched z-sampling serves as the ground truth,
since the full KG~ODE with 128 slices per cell is prohibitively expensive for repeated frozen-phonon runs.

Figure~\ref{fig:fp_cbed} shows the frozen-phonon averaged CBED patterns for each method on a logarithmic intensity scale.
Figure~\ref{fig:fp_diff_maps} presents difference maps of each method's averaged diffraction pattern and exit-wave amplitude
relative to the KG~FWD reference.
Table~\ref{tab:fp_errors} lists the RMSE, relative $L^2$ error, and mean wall-clock time per frozen phonon configuration.

Even in this realistic scenario with thermal disorder, the angular spectrum method produces diffraction patterns
that are effectively identical to the KG~FWD reference, while requiring a small fraction of the compute time.
These results confirm that across all tested regimes---from single-beam verification to frozen-phonon averaged CBED---the
angular spectrum propagation kernel is sufficient to capture the physics of high-angle electron scattering
without recourse to more expensive methods.

\begin{figure}[ht]
    \centering
    \safeincludegraphics[width=\textwidth,keepaspectratio]{fp_cbed_patterns}
    \caption{Frozen-phonon averaged CBED patterns for Si~[111] at 200~keV (6.2~mrad probe, 16 configurations,
             16~sl/cell, 120 unit cells). Intensities displayed on a logarithmic scale.}
    \label{fig:fp_cbed}
\end{figure}

\begin{figure}[ht]
    \centering
    \safeincludegraphics[width=\textwidth,keepaspectratio]{fp_diff_maps}
    \caption{Difference maps relative to KG~FWD Lanczos for Si~[111] frozen-phonon CBED.
             Top row: normalised diffraction pattern difference.
             Bottom row: normalised exit-wave amplitude difference.}
    \label{fig:fp_diff_maps}
\end{figure}

\begin{table}[ht]
    \centering
    \begin{tabular}{lccc}
        \hline
        \textbf{Method} & \textbf{RMSE} & \textbf{Rel.\ $L^2$} & \textbf{Time/config (s)} \\
        \hline
        Fresnel MS              & $8.26\times10^{-5}$ & $2.39\times10^{-3}$ & 0.82 \\
        Angular Spectrum MS     & $8.31\times10^{-5}$ & $2.40\times10^{-3}$ & 0.44 \\
        KG FWD Lanczos (ref)    & ---                 & ---                 & 36.36 \\
        \hline
    \end{tabular}
    \caption{Error metrics and runtime for frozen-phonon averaged CBED of Si~[111] relative to KG~FWD~Lanczos.}
    \label{tab:fp_errors}
\end{table}

\section{Conclusion}
We presented a systematic comparison of four propagation methods for electron multislice simulation---Fresnel multislice,
angular spectrum multislice, the Wave Propagation Method, and a Krylov--Lanczos Klein-Gordon forward solver---benchmarked
across four experiments of increasing physical complexity: high-angle beam amplitude verification against published results,
z-sampling convergence, convergent-beam diffraction at probe semi-angles up to 100~mrad, and frozen-phonon CBED of Si~[111].

Our principal finding is that replacing the Fresnel propagation kernel with the exact angular spectrum kernel---a minimal
and inexpensive modification to standard multislice---is sufficient to capture high-angle scattering with accuracy
comparable to the far more computationally expensive WPM and Klein-Gordon solvers.
The WPM and Lanczos methods do not permit coarser z-sampling for a given accuracy level,
and the diminishing returns beyond moderate z-refinement make them impractical for routine use.

These results do not diminish the theoretical value of the WPM or full Klein-Gordon solvers:
they remain essential as ground-truth references and may prove necessary in extreme regimes not tested here
(e.g., very thick high-Z samples at energies below 100~keV).
However, for the broad range of conditions encountered in modern TEM---including convergent-beam
and frozen-phonon simulations---the angular spectrum multislice method provides an excellent balance
of accuracy and computational efficiency.

The differentiable refractive-index binning strategy introduced for the WPM remains a useful contribution,
as it enables gradient-based inverse problems such as ptychography to be formulated with a wide-angle
forward model when needed. Future work will explore the limits of the angular spectrum approximation
in extreme scattering geometries and integrate the method with large-scale ptychographic reconstructions.

\bibliographystyle{unsrt}
\bibliography{sample}

\appendix

\section{Differentiable refractive-index binning for WPM multislice}
\label{sec:binning}

To make the computation of the wave propagation method tractable and limit the need for $O(N^{2D})$ computations, we adapt the WPM algorithm presented in \cite{DiffWPM2025} which
uses a refractive index binning strategy to limit the number of unique refractive index values in a slice while maintaining differentiability with respect to the refractive index map $n(x,y)$.

To implement this method, we first discretize the refractive index range $[n_{\mathrm{min}}, n_{\mathrm{max}}]$ of the current slice into $N_{\mathrm{bins}}$ discrete reference values, denoted as
$\{n_{\mathrm{ref}}^{(k)}\}_{k=1}^{N_{\mathrm{bins}}}$.

In a typical multislice potential, many of the refractive index values will be very close to $1$ (vacuum region), while the refractive index values of an atom
will concentrate in a narrow range slightly above $1$. Since there are likely to be many more refractive index values near the vacuum region,
we employ a quadratic distribution for the reference indices rather than using linearly spaced bins in order to allocate more bins near $n=1$.
The reference indices are distributed according to a quadratic function determined by the parameter $\gamma$, with $\gamma=2.0$ for quadratic spacing:

\[
n_{\mathrm{ref}}^{(k)} = n_{\mathrm{min}} + (n_{\mathrm{max}} - n_{\mathrm{min}}) \left( \frac{k}{N_{\mathrm{bins}}-1} \right)^\gamma
\]

which allows for denser binning near the vacuum refractive index when $\gamma > 1$.

Next, for a propagation step $\Delta z$, we compute a bank of propagated fields $u_k(x,y)$. Each field $u_k$ corresponds to the
diffraction of the input field $u_{\mathrm{in}}(x,y)$ assuming a homogeneous medium with refractive index $n_{\mathrm{ref}}^{(k)}$.
This is performed efficiently via batch FFT operations using the angular spectrum kernel:

\[
u_k(x,y) = \mathcal{F}^{-1} \left( \mathcal{F}(u_{\mathrm{in}}) e^{i \Delta z \sqrt{(k_0 n_{\mathrm{ref}}^{(k)})^2 - k_x^2 - k_y^2}} \right)
\]

To evaluate the field at a specific spatial coordinate $(x,y)$ where the local refractive index is $n(x,y)$,
we identify the two bounding reference bins such that $n_L \le n(x,y) < n_R$, where $n_L, n_R \in \{n_{\mathrm{ref}}^{(k)}\}$.
We adopt a differentiable interpolation scheme by defining a local interpolation weight

\[
w_{\mathrm{raw}} = \frac{n(x,y) - n_L}{n_R - n_L}, \quad w_{\mathrm{raw}} \in [0, 1]
\]

To ensure smooth gradients and minimize discretization artifacts, we map this linear weight through a smoothstep activation function $S(w)$,
akin to the spatial partitioning technique demonstrated by \cite{BarreBrunel} via a cubic Hermite interpolation function:

\[
w = S(w_{\mathrm{raw}}) = 3w_{\mathrm{raw}}^2 - 2w_{\mathrm{raw}}^3
\]
While linear interpolation would satisfy the requirement for differentiability, it could result in discontinuities in the gradient at the bin boundaries in a gradient-based optimization scheme.

The final field at the next step $u(x,y, z+\Delta z)$ is computed as the weighted superposition of the fields
propagated at the bounding indices:

\[
u(x,y) = (1 - w) \cdot u_L(x,y) + w \cdot u_R(x,y)
\]

This formulation ensures that the propagation model is fully differentiable with respect to the refractive index map $n(x,y)$, enabling inverse reconstruction of potentials via automatic differentiation,
and that only a limited number of FFTs (equal to $N_{\mathrm{bins}}$) are required per propagation step, significantly reducing the computational cost compared to a naive WPM implementation.
The accuracy of this binned WPM multislice method depends on the number of bins $N_{\mathrm{bins}}$ chosen and will change on a case-by-case basis depending on the refractive index distribution of the sample.
One can mitigate this potential source of error by performing a test simulation and doubling the number of bins until no difference is observed in the output wavefunction.

In general, the WPM will need a GPU for efficient computation, and we have found that $N_{\mathrm{bins}}$ between 64 and 512 are sufficient for the simulations presented in this work.
This is a marked improvement over the naive case, as it enables the WPM to simulate many slices with larger grid sizes ($512 \times 512$ or $1024 \times 1024$ pixels)
in a reasonable time frame (seconds to minutes), provided the refractive index binning strategy is capable of correctly capturing the refractive index distribution of the sample.

\end{document}
